package string.stringToIntConverter;

public class Split_ReplaceAll {

    /* String name="hello im rutwik
           o/p => ["hello", "im", "rutwik"]

  1)  split(" ")  =>
                  1. divide word into string Array saperate each word,
                       (" ") space means saperate word into string where space available.
                  2.   (",") saperate word into string Array where "," available in String.
                  3

                  // those word add in split("i")between double coats it remove those word from full line of string
                      and saperate the words from that character.

   2) name.replaceAll("t","z") :
                  => in string where t is available those character "t" replace by "z".

     */
}
